var conf = {
    //Represents the path where the configuration will saved and taken of
    //Representa o caminho de onde as configurações serão salvas e lidas
    "path": "/usr/share/cockpit/ippit/settings.json" 
}
export default conf;